npm install socket.io
npm install sqlite3
npm install fluent-ffmpeg multer socket.io
npm install @ffmpeg-installer/ffmpeg @ffprobe-installer/ffprobe
npm install express multer slugify ffprobe ffprobe-static better-sqlite3 ws
pip install diffusers transformers torch pillow
# Install OpenCV
pip install opencv-python

# Install PySceneDetect
pip install scenedetect
